# 📁 Структура проекта ÉTT Market

```
salut/
│
├── app/                                # Next.js App Router (страницы)
│   ├── category/
│   │   └── [category]/
│   │       └── page.tsx               # Динамическая страница категории
│   ├── layout.tsx                     # Root layout (Header + Footer)
│   ├── page.tsx                       # Главная страница
│   ├── not-found.tsx                  # 404 страница
│   └── globals.css                    # Глобальные стили
│
├── components/                        # React компоненты
│   ├── Header.tsx                     # Хедер с навигацией
│   ├── Footer.tsx                     # Футер
│   ├── ProductCard.tsx                # Карточка товара
│   └── ProductGrid.tsx                # Сетка карточек
│
├── types/                             # TypeScript типы + русские лейблы категорий
│   └── product.ts                     # Типы Category и Product, categoryLabels
│
├── lib/                               # Логика/утилиты
│   ├── supabaseClient.ts              # Клиент Supabase
│   ├── products.ts                    # Работа с продуктами через Supabase
│   └── types.ts                       # Общие типы
│
├── public/                            # Статические файлы (изображения)
│
├── node_modules/                      # Зависимости (не коммитятся)
├── .next/                             # Build файлы (не коммитятся)
│
├── tailwind.config.ts                 # Конфиг Tailwind CSS
├── postcss.config.mjs                 # Конфиг PostCSS
├── next.config.ts                     # Конфиг Next.js
├── tsconfig.json                      # Конфиг TypeScript
├── package.json                       # Зависимости и скрипты
├── .gitignore                         # Git ignore
│
├── README.md                          # Полная документация
├── QUICK_START.md                     # Быстрый старт
└── PROJECT_STRUCTURE.md               # Этот файл
```

---

## 🎯 Назначение файлов

### Страницы (app/)

| Файл                                  | URL                       | Назначение                          |
| ------------------------------------- | ------------------------- | ----------------------------------- |
| `app/page.tsx`                        | `/`                       | Главная страница                    |
| `app/category/[category]/page.tsx`    | `/category/gadgets`       | Страница категории (динамическая)   |
| `app/not-found.tsx`                   | `/404`                    | Страница ошибки 404                 |
| `app/layout.tsx`                      | —                         | Общий layout для всех страниц       |
| `app/globals.css`                     | —                         | Глобальные CSS стили                |

### Компоненты (components/)

| Файл                       | Назначение                                          |
| -------------------------- | --------------------------------------------------- |
| `Header.tsx`               | Хедер с логотипом и навигацией по категориям       |
| `Footer.tsx`               | Футер с описанием проекта                           |
| `ProductCard.tsx`          | Карточка товара (повторно используемый компонент)   |
| `ProductGrid.tsx`          | Адаптивная сетка карточек                           |

### Данные (Supabase)

| Файл/таблица               | Назначение                                          |
| -------------------------- | --------------------------------------------------- |
| `lib/products.ts`          | Получение/поиск товаров из таблицы `products`       |
| `lib/supabaseClient.ts`    | Инициализация Supabase клиента                      |
| Таблица `products` в БД    | Хранение всех товаров                               |

### Типы (types/)

| Файл                       | Назначение                                          |
| -------------------------- | --------------------------------------------------- |
| `product.ts`               | TypeScript типы и интерфейсы                        |

**Экспорты:**
- `type Category` — тип категории
- `interface Product` — интерфейс товара
- `categoryLabels` — маппинг категорий на русский
- `categoryFromSlug()` — получить категорию из URL slug

### Конфигурация

| Файл                       | Назначение                                          |
| -------------------------- | --------------------------------------------------- |
| `tailwind.config.ts`       | Кастомные цвета, шрифты, расширения Tailwind        |
| `postcss.config.mjs`       | PostCSS плагины (Tailwind + Autoprefixer)           |
| `next.config.ts`           | Конфиг Next.js (remote patterns для изображений)    |
| `tsconfig.json`            | Конфиг TypeScript (пути, строгость)                 |
| `package.json`             | Зависимости, скрипты, метаданные                    |

---

## 🔄 Как данные попадают на страницы

### Главная страница (/)

```
Supabase → lib/products.ts (getAllProducts)
       ↓ 
app/page.tsx (получает последние товары)
       ↓
components/ProductGrid.tsx (отображает сетку)
       ↓
components/ProductCard.tsx (отображает карточки)
```

### Страница категории (/category/gadgets)

```
URL параметр: "gadgets"
       ↓
app/category/[category]/page.tsx (получает параметр)
       ↓
Supabase → lib/products.ts (getProductsByCategory("gadgets"))
       ↓
components/ProductGrid.tsx (отображает сетку)
       ↓
components/ProductCard.tsx (отображает карточки)
```

---

## 📝 Важные особенности

### 1. Next.js App Router

Проект использует новый App Router из Next.js 15:
- Файловая маршрутизация в папке `app/`
- Server Components по умолчанию
- Динамические параметры через `[param]/`

### 2. TypeScript

Все файлы типизированы:
- `.tsx` — React компоненты
- `.ts` — TypeScript модули
- Строгая типизация для безопасности

### 3. Tailwind CSS

Utility-first подход к стилям:
- Кастомные цвета в `tailwind.config.ts`
- Минимум кастомного CSS
- Адаптивность через встроенные классы

### 4. Данные в Supabase

Товары хранятся в таблице `products` в Supabase:
- CRUD из админки `/admin`
- Без деплоя для обновления данных
- Можно расширять схему и права через Supabase

---

## 🚀 Куда добавлять код

### Хотите добавить новый компонент?
→ Создайте файл в `components/`

### Хотите добавить новую страницу?
→ Создайте файл в `app/`

### Хотите изменить типы?
→ Редактируйте `types/product.ts`

### Хотите изменить цвета?
→ Редактируйте `tailwind.config.ts`

---

Более подробная информация в [README.md](README.md)
