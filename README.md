# ÉTT Market — витрина избранных товаров

ÉTT Market — минималистичная витрина с товарами из российских маркетплейсов (Ozon, Wildberries, Яндекс Маркет и др.). Мы ничего не продаём: каждая карточка ведёт на внешний сайт продавца.

- Чистый бело‑серый интерфейс без корзины и оплаты  
- Категории и сортировки на русском, внутренние ключи — на английском  
- Данные хранятся в Supabase (`products`), редактируются через админку `/admin`

## 🚀 Запуск

```bash
npm install
npm run dev
```

Сборка: `npm run build` → `npm start`  
Линт: `npm run lint`

Переменные окружения (`.env.local`):
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `ADMIN_PASSWORD`

## 📂 Структура

```
app/                # Страницы (Next.js App Router)
  layout.tsx        # Общий layout (Header + Footer)
  page.tsx          # Главная
  category/[category]/page.tsx  # Категории
  product/[id]/page.tsx         # Детальная страница
  admin/            # Админка (login + CRUD)
components/         # Header, Footer, ProductCard, ProductGrid, ProductDetail
lib/                # supabaseClient, products helpers, типы
types/              # categoryLabels и утилиты для категорий
```

## 🗂 Данные и категории

- Таблица `products` в Supabase хранит все товары.  
- Поля и типы описаны в `lib/types.ts`.  
- Категории в базе: `clothing`, `accessories`, `gadgets`, `home`, `apps`.  
- Русские лейблы хранятся в `types/product.ts` (`categoryLabels`).

## ✏️ Админка

- URL: `/admin` (пароль `ADMIN_PASSWORD`).  
- Возможности: поиск/фильтр, создание, редактирование, удаление, сортировка изображений, поддержка видео `.mp4` (циклично без контролов).  
- Все действия сразу пишут в Supabase и отражаются на сайте без деплоя.

## 🧭 UI и тексты

- Лого и метаданные: **ÉTT Market**.  
- Навигация и кнопки: полностью на русском.  
- Кнопка товара: «Перейти к товару».  
- Метки: «Реклама» / «Партнерский товар».

## ⚙️ Технологии

Next.js 15 (App Router), TypeScript, Supabase, Tailwind CSS, React 18.

---

ÉTT Market — витрина избранных товаров с российских маркетплейсов.
