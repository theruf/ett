# ÉTT Market — Curated витрина интересных вещей

ÉTT Market — это **НЕ интернет-магазин** и **НЕ маркетплейс**.

Это минималистичная curated-витрина, где я как куратор отбираю интересные товары, гаджеты, приложения, книги, музыку, фильмы и сервисы.

Сайт ничего не продаёт напрямую. При клике на кнопку "Перейти к товару" пользователь переходит на внешний сайт продавца (Ozon, Яндекс.Маркет, Amazon, App Store и т.д.).

## 🎯 Концепция

- **Минимализм**: чистый бело-серый дизайн без лишних элементов
- **Без корзины и оплаты**: только карточки товаров с внешними ссылками
- **Curated подход**: ценность в отборе и вкусе куратора
- **Расширяемость**: легко добавлять новые товары и категории
- **Монетизация**: поддержка реферальных ссылок и спонсорского размещения

---

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Сайт будет доступен по адресу: `http://localhost:3000`

### Сборка для продакшена

```bash
npm run build
npm start
```

---

## 📁 Структура проекта

```
urldrop/
├── app/                        # Next.js App Router
│   ├── category/
│   │   └── [category]/        # Динамические страницы категорий
│   │       └── page.tsx
│   ├── layout.tsx             # Общий layout с Header и Footer
│   ├── page.tsx               # Главная страница
│   ├── not-found.tsx          # Страница 404
│   └── globals.css            # Глобальные стили
│
├── components/                # React компоненты
│   ├── Header.tsx             # Хедер с навигацией
│   ├── Footer.tsx             # Футер
│   ├── ProductCard.tsx        # Карточка товара
│   └── ProductGrid.tsx        # Сетка карточек
│
├── data/                      # Данные приложения
│   └── products.ts            # Mock данные товаров
│
├── types/                     # TypeScript типы
│   └── product.ts             # Типы Category и Product
│
├── public/                    # Статические файлы
│
├── tailwind.config.ts         # Конфиг Tailwind (кастомные цвета)
├── tsconfig.json              # Конфиг TypeScript
├── next.config.ts             # Конфиг Next.js
└── package.json
```

---

## 🎨 Цветовая палитра

Проект использует минималистичную бело-серую палитру:

| Название          | Hex код   | Использование                |
| ----------------- | --------- | ---------------------------- |
| `white`           | `#FFFFFF` | Основной фон, карточки       |
| `gray-lightest`   | `#F7F7F7` | Фон страницы                 |
| `gray-light`      | `#EDEDED` | Разделители, рамки           |
| `gray-text`       | `#9A9A9A` | Второстепенный текст         |
| `gray-dark`       | `#111111` | Основной текст, заголовки    |

Цвета настроены в [tailwind.config.ts](tailwind.config.ts).

---

## ➕ Как добавить новый товар

Товары теперь живут в Supabase (таблица `products`). Добавление делается через админку `/admin`:

1. Зайти на `/admin` и залогиниться паролем `ADMIN_PASSWORD` (см. `.env.local`).
2. Нажать “Add product” или “Edit” в списке.
3. Заполнить поля, указать ссылки на картинки (Supabase Storage или любой внешний CDN) и сохранить.
4. Сразу после сохранения товар появляется на сайте — без деплоя.

---

## 📂 Как добавить новую категорию

### Шаг 1: Добавьте категорию в тип

Откройте [types/product.ts](types/product.ts) и добавьте новую категорию:

```typescript
export type Category =
  | "clothing"
  | "accessories"
  | "gadgets"
  | "home"
  | "books"
  | "music"
  | "movies"
  | "services"
  | "your-new-category"; // ← Добавьте вашу категорию
```

### Шаг 2: Добавьте русское название

В том же файле добавьте маппинг:

```typescript
export const categoryLabels: Record<Category, string> = {
  clothing: "Одежда",
  accessories: "Аксессуары",
  gadgets: "Гаджеты",
home: "Home",
  books: "Книги",
  music: "Музыка",
  movies: "Кино",
  services: "Сервисы",
  "your-new-category": "Ваша категория", // ← Добавьте перевод
};
```

### Шаг 3: Добавьте категорию в навигацию

Откройте [components/Header.tsx](components/Header.tsx) и добавьте категорию в массив:

```typescript
const categories: Category[] = [
  "clothing",
  "accessories",
  "gadgets",
  "home",
  "books",
  "music",
  "movies",
  "services",
  "your-new-category", // ← Добавьте здесь
];
```

### Шаг 4: Добавьте категорию на главную страницу

Откройте [app/page.tsx](app/page.tsx) и добавьте категорию:

```typescript
const categories: Category[] = [
  "clothing",
  "accessories",
  "gadgets",
  "home",
  "books",
  "music",
  "movies",
  "services",
  "your-new-category", // ← Добавьте здесь
];
```

### Шаг 5: Добавьте категорию в generateStaticParams

Откройте [app/category/[category]/page.tsx](app/category/[category]/page.tsx):

```typescript
export async function generateStaticParams() {
  const categories: Category[] = [
    "clothing",
    "accessories",
    "gadgets",
    "home",
    "books",
    "music",
    "movies",
    "services",
    "your-new-category", // ← Добавьте здесь
  ];

  return categories.map((category) => ({
    category,
  }));
}
```

### Готово!

Теперь можно добавлять товары с новой категорией, и они будут отображаться на сайте.

---

## 🔗 Реферальные ссылки и спонсорство

### Реферальные ссылки (Affiliate)

Чтобы отметить товар как реферальный, установите:

```typescript
{
  isAffiliate: true,
  // ...
}
```

В карточке товара появится маленькая надпись "Affiliate".

### Спонсорские товары (Sponsored)

Чтобы отметить товар как спонсорский:

```typescript
{
  isSponsored: true,
  // ...
}
```

В правом верхнем углу карточки появится метка "Sponsored".

---

## 🎨 Кастомизация дизайна

### Изменить цвета

Откройте [tailwind.config.ts](tailwind.config.ts) и измените цвета в секции `colors`:

```typescript
colors: {
  white: "#FFFFFF",
  "gray-lightest": "#F7F7F7",
  "gray-light": "#EDEDED",
  "gray-text": "#9A9A9A",
  "gray-dark": "#111111",
}
```

### Изменить шрифт

По умолчанию используется `Inter`. Чтобы изменить:

1. Откройте [app/layout.tsx](app/layout.tsx)
2. Импортируйте нужный шрифт из `next/font/google`
3. Примените к `<body>`

---

## 📦 Компоненты

### ProductCard

Карточка товара с изображением, названием, описанием и кнопкой.

**Пропсы:**

- `product: Product` — объект товара

**Использование:**

```tsx
import ProductCard from "@/components/ProductCard";

<ProductCard product={product} />;
```

### ProductGrid

Адаптивная сетка карточек товаров.

**Пропсы:**

- `products: Product[]` — массив товаров

**Использование:**

```tsx
import ProductGrid from "@/components/ProductGrid";

<ProductGrid products={products} />;
```

### Header

Хедер с логотипом и навигацией по категориям.

### Footer

Минималистичный футер с описанием проекта.

---

## 🔮 Планы на будущее

### Подключение базы данных

Товары уже вынесены в Supabase (PostgreSQL) и подтягиваются на сайт без деплоя:
- Таблица `products` хранит все товары
- Админка `/admin` пишет/читает в Supabase
- Данные доступны на проде сразу после сохранения

### Админка

Для удобного добавления товаров можно создать админ-панель:

- Форма добавления товаров
- Загрузка изображений
- Редактирование/удаление товаров
- Управление категориями

### Фильтры и поиск

- Поиск по названию
- Фильтрация по источнику
- Сортировка по дате добавления
- Теги для товаров

### Аналитика

- Отслеживание кликов по товарам
- Самые популярные категории
- Конверсия по реферальным ссылкам

---

## 🛠 Технологии

- **Next.js 15** — React framework с App Router
- **TypeScript** — типизация
- **Tailwind CSS** — утилитарные стили
- **React 18** — UI библиотека

---

## 📝 Лицензия

Это учебный проект. Используйте как хотите.

---

## 👨‍💻 Автор

Разработано с использованием Claude Code.

ÉTT Market — минималистичная витрина без лишнего.
