# 🚀 ÉTT Market — быстрый старт

## 1. Установите зависимости

```bash
npm install
```

## 2. Настройте окружение

Создайте `.env.local`:
```
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
ADMIN_PASSWORD=...
```

## 3. Запустите проект

```bash
npm run dev
```

Локально: http://localhost:3000

Сборка/проверка:
```bash
npm run lint
npm run build
npm start
```

## 4. Добавляйте товары

1. Откройте `/admin`, введите пароль (`ADMIN_PASSWORD`).
2. Создайте товар: название, категория (`clothing`, `accessories`, `gadgets`, `home`, `apps`), цена/валюта, описания, изображения (URL или mp4), ссылка на маркетплейс, метки «Реклама»/«Партнерский товар».
3. Сохраните — товар сразу появится на сайте (данные читаются из Supabase без деплоя).

## 5. Категории и лейблы

- В базе остаются английские ключи категорий.  
- Русские подписи лежат в `types/product.ts` (`categoryLabels`).  
- Навигация и сортировки уже на русском.

**Готово!** ÉTT Market работает как витрина избранных товаров.
